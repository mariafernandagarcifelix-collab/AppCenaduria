<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="AppCenaduria.Views.Reportes"
             Title="Reporte de Ventas"
             BackgroundColor="#0A0A0A">

    <ScrollView>
        <VerticalStackLayout Padding="0" Spacing="0">

            <Border StrokeThickness="0" BackgroundColor="#111111" Padding="24,28,24,20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="0,0,28,28"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,8" Radius="20" Opacity="0.4"/>
                </Border.Shadow>
                <VerticalStackLayout Spacing="4">
                    <Label Text="📊  Reporte" FontSize="24" FontAttributes="Bold" TextColor="#F5F5F5" HorizontalOptions="Center" CharacterSpacing="0.5"/>
                    <Label Text="Resumen de ventas del negocio" TextColor="#555555" FontSize="12" HorizontalOptions="Center"/>
                    <BoxView HeightRequest="3" Color="#fc4b08" WidthRequest="40" HorizontalOptions="Center" Margin="0,6,0,0">
                        <BoxView.Shadow>
                            <Shadow Brush="#fc4b08" Offset="0,0" Radius="8" Opacity="0.5"/>
                        </BoxView.Shadow>
                    </BoxView>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Padding="16,20,16,16" Spacing="16">

                <Grid ColumnDefinitions="*, *" ColumnSpacing="12">
                    <Border Grid.Column="0" StrokeThickness="2" Stroke="#fc4b08" BackgroundColor="#111111" Padding="16">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="18"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="#fc4b08" Offset="0,0" Radius="16" Opacity="0.15"/>
                        </Border.Shadow>
                        <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
                            <Label Text="🔥" FontSize="22" HorizontalOptions="Center"/>
                            <Label Text="Hoy" TextColor="#777777" FontSize="11" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label x:Name="lblVentasHoy" Text="$0.00" FontSize="22" FontAttributes="Bold" TextColor="#4CAF50" HorizontalOptions="Center"/>
                            <Label x:Name="lblPedidosHoy" Text="0 pedidos" TextColor="#555555" FontSize="10" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Column="1" StrokeThickness="1" Stroke="#1E1E1E" BackgroundColor="#111111" Padding="16">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="18"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="Black" Offset="0,4" Radius="12" Opacity="0.25"/>
                        </Border.Shadow>
                        <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
                            <Label Text="📅" FontSize="22" HorizontalOptions="Center"/>
                            <Label Text="Este Mes" TextColor="#777777" FontSize="11" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label x:Name="lblVentasMes" Text="$0.00" FontSize="22" FontAttributes="Bold" TextColor="#F0F0F0" HorizontalOptions="Center"/>
                            <Label x:Name="lblPedidosMes" Text="0 pedidos" TextColor="#555555" FontSize="10" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <Border StrokeThickness="1" Stroke="#1E1E1E" BackgroundColor="#111111" Padding="16,14">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18"/>
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="12">
                        <Label Text="FILTRAR POR PERIODO" TextColor="#666666" FontSize="10" FontAttributes="Bold" CharacterSpacing="1"/>
                        <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                            <HorizontalStackLayout Spacing="8">
                                <Button Text="Hoy" Clicked="OnChipHoyClicked" BackgroundColor="#252525" TextColor="White" CornerRadius="14" HeightRequest="32" Padding="16,0" FontSize="11" FontAttributes="Bold"/>
                                <Button Text="Esta Semana" Clicked="OnChipSemanaClicked" BackgroundColor="#1A1A1A" TextColor="#AAAAAA" CornerRadius="14" HeightRequest="32" Padding="16,0" FontSize="11"/>
                                <Button Text="Este Mes" Clicked="OnChipMesClicked" BackgroundColor="#1A1A1A" TextColor="#AAAAAA" CornerRadius="14" HeightRequest="32" Padding="16,0" FontSize="11"/>
                            </HorizontalStackLayout>
                        </ScrollView>

                        <Grid ColumnDefinitions="*, *, Auto" ColumnSpacing="10">
                            <Border Grid.Column="0" StrokeThickness="1" Stroke="#252525" BackgroundColor="#0D0D0D" Padding="10,8">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12"/>
                                </Border.StrokeShape>
                                <VerticalStackLayout>
                                    <Label Text="Desde" TextColor="#555555" FontSize="10"/>
                                    <DatePicker x:Name="dpInicio" BackgroundColor="Transparent" TextColor="White" Format="dd/MM/yy"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Column="1" StrokeThickness="1" Stroke="#252525" BackgroundColor="#0D0D0D" Padding="10,8">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12"/>
                                </Border.StrokeShape>
                                <VerticalStackLayout>
                                    <Label Text="Hasta" TextColor="#555555" FontSize="10"/>
                                    <DatePicker x:Name="dpFin" BackgroundColor="Transparent" TextColor="White" Format="dd/MM/yy"/>
                                </VerticalStackLayout>
                            </Border>

                            <Button Grid.Column="2" Text="🔍" BackgroundColor="#fc4b08" TextColor="White" VerticalOptions="End" FontAttributes="Bold" Clicked="OnFiltrarClicked" WidthRequest="48" HeightRequest="48" CornerRadius="14" Padding="0" FontSize="18">
                                <Button.Shadow>
                                    <Shadow Brush="#fc4b08" Offset="0,3" Radius="10" Opacity="0.3"/>
                                </Button.Shadow>
                            </Button>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Border StrokeThickness="1" Stroke="#1E3A1E" BackgroundColor="#0D1A0D" Padding="14,12">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="14"/>
                    </Border.StrokeShape>
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                        <Label Text="💰" FontSize="16" VerticalOptions="Center"/>
                        <Label Text="Total filtrado:" TextColor="#AAAAAA" FontSize="13" VerticalOptions="Center"/>
                        <Label x:Name="lblTotalFiltrado" Text="$0.00" TextColor="#4CAF50" FontAttributes="Bold" FontSize="20" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </Border>

                <Label Text="RESULTADOS" TextColor="#555555" FontSize="10" FontAttributes="Bold" CharacterSpacing="1.5" Margin="4,6,0,0"/>

                <Grid HeightRequest="300">
                    <syncfusion:SfDataGrid x:Name="listaResumen"
                                           AutoGenerateColumnsMode="None"
                                           ColumnWidthMode="Fill"
                                           HeaderRowHeight="0"
                                           RowHeight="75"
                                           BackgroundColor="Transparent"
                                           GridLinesVisibility="None"
                                           SelectionMode="None">
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:DataGridTemplateColumn MappingName="Folio">
                                <syncfusion:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border StrokeThickness="1" Stroke="#1E1E1E" BackgroundColor="#111111" Margin="0,0,0,8" Padding="14,12">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="14"/>
                                            </Border.StrokeShape>
                                            <Grid ColumnDefinitions="*, Auto">
                                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                                    <Label Text="{Binding Folio, StringFormat='Orden #{0}'}" TextColor="#F0F0F0" FontAttributes="Bold" FontSize="14"/>
                                                    <Label Text="{Binding FechaPedido, StringFormat='{0:dd/MM/yyyy hh:mm tt}'}" TextColor="#666666" FontSize="11"/>
                                                </VerticalStackLayout>
                                                <Label Grid.Column="1" Text="{Binding Total, StringFormat='${0:F2}'}" TextColor="#4CAF50" FontAttributes="Bold" FontSize="16" VerticalOptions="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </syncfusion:DataGridTemplateColumn.CellTemplate>
                            </syncfusion:DataGridTemplateColumn>
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>

                    <VerticalStackLayout x:Name="vistaVaciaReportes" IsVisible="False" HorizontalOptions="Center" Spacing="10" Margin="0,40,0,0">
                        <Label Text="📭" FontSize="36" HorizontalOptions="Center"/>
                        <Label Text="No hay ventas registradas" TextColor="#555555" FontSize="14" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Grid>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>